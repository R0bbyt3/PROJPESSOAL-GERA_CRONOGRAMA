function salvarDadosPessoais() {
    const nome = document.getElementById('nome').value;
    const sobrenome = document.getElementById('sobrenome').value;
    // Salve outros dados conforme necessário
    console.log('Dados Salvos:', nome, sobrenome);
}

function adicionarMateria() {
    const nomeMateria = document.getElementById('nome-materia').value;
    const creditos = document.getElementById('creditos').value;
    // Adicionar lógica para salvar matéria
    console.log('Matéria Adicionada:', nomeMateria, creditos);
}

// Funções adicionais para manipulação de prioridades e outras configurações
